spring:
  application:
    name: water-article
  cloud:
    config:
      discovery:
        enabled: true
        service: configerserver

eureka:
  client:
    serviceUrl:
      defaultZone: http://machinedog.yipzale.me:1111/eureka/
  instance:
    lease-expiration-duration-in-seconds: 15
    lease-renewal-interval-in-seconds: 5